Phase 1 public increment record (redacted)

Historical August 28 builder record. Superseded by 2026-09-04-verification.md
for current code, tests, Docker startup, build-context checks and persistence.

Branch: phase1-sanitized
origin/main (ancestor): c2964b5868eccf593844336096f99a0f55914675
Phase 1 squash: 089ed0f2821d839192897d286ade89aff95adb39
SQLite doctor: ff56668911b98208c202e6275d5db210a3962acf
Docker allowlist: 8e8f19989604e06efe032bb343dd237bb8f165a7
Verification record: 0ee257e3a9059641f7b76160035e849e8f101b68
Date: 2026-08-28

Commands (all exit 0 unless noted):
- uv sync --all-groups --frozen
- uv run ruff format --check app tests scripts
- uv run ruff check app tests scripts
- uv run mypy app
- uv run pytest -q --cov=app
  Result: 482 passed, app coverage 88% (88.03%, fail_under 85)
- uv run pre-commit run --all-files
- docker compose config -q (exit 0)
- git diff --check origin/main...HEAD
- gitleaks git . --log-opts=HEAD --redact --no-banner (104 commits, no leaks)
- uv run agent-call doctor --dummy
- uv run python -m app doctor --dummy

Gating tests:
- tests/test_evaluation_start.py (live_calls_disabled; zero Twilio/OpenAI I/O;
  17 passed)
- tests/test_doctor.py (missing/blank/malformed; sqlite /dev/null, directory,
  corrupt file, connect/write failure, existing DB unchanged, new path with no
  artifacts, zero-byte file as new location, missing parent conservative
  failure; invalid origin; mocked healthy HTTPS; no secret print; 34 passed)
- tests/test_container_security.py (dockerignore exists; COPY sources kept;
  .git/.env/.venv/caches/coverage/databases excluded; no COPY . .; no secret
  config in COPY; 8 passed)
- tests/test_fork_safety.py (golden paths plus upstream-maintainer-only workflow
  job guard; 5 passed)
- tests/test_smoke_prepare.py (initialize, seven tools, prepare, never start;
  absolute/scheme-relative MCP paths and remote HTTP rejected before credential
  read; loopback HTTP and remote HTTPS MockTransport; 19 passed)

Dummy boot (source):
- AGENT_CALL_PROFILE=evaluation uvicorn app.main:app --host 127.0.0.1 --port 8767
- GET /healthz twice -> {"status":"ok"}
  See healthz-1.json healthz-2.json
  (source dummy boot from the prior increment; not re-run in this increment)

Compose / image build:
- docker compose version v2.36.0-desktop.1
- docker compose config -q -> 0
- docker compose up --build skipped: Docker daemon not running
  See compose-launch.log
- Persistence-after-restart not exercised
- Actual image build and context packing are unverified (daemon offline).
  .dockerignore matching is covered by tests/test_container_security.py.

History (verified on this branch at 8e8f19989604e06efe032bb343dd237bb8f165a7):
- git merge-base --is-ancestor origin/main HEAD
- test ! -e docs/managed-self-hosted-product-plan.md
- test -z "$(git log origin/main..HEAD --format='%H' -- docs/managed-self-hosted-product-plan.md)"
- ! git rev-list origin/main..HEAD --objects | rg 'docs/managed-self-hosted-product-plan\.md'
- ! git cat-file -e HEAD:docs/managed-self-hosted-product-plan.md
- Private managed plan remains outside this public repository.
- Local branches main and phase1-unsafe-source were not modified
  (still b344c82438483254fe2f772c1f99b4221dd7b892).

No secrets, full E.164 numbers, transcripts, or customer identifiers in this file.
No call, deploy, webhook change, provider purchase, or push in this increment.
